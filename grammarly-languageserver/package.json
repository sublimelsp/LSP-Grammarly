{
  "name": "lsp-grammarly",
  "private": true,
  "dependencies": {
    "pnpm": "^7.1.0"
  },
  "scripts": {
    "postinstall": "git clone https://github.com/znck/grammarly grammarly-languageserver ; which pnpm ; cd grammarly-languageserver ; git apply ../console.patch ; which pnpm ; NODE_ENV= pnpm i ; pnpm run build"
  }
}
